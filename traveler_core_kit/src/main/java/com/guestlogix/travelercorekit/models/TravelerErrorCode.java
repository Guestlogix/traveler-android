package com.guestlogix.travelercorekit.models;

public enum TravelerErrorCode {
    SDK_NOT_INITIALIZED, BAD_URL, CONNECTION_ERROR, NO_REQUEST, FORBIDDEN, UNAUTHORIZED, SERVER_ERROR, PARSING_ERROR, NO_DATE, NO_TIME,
    //Mapping Error codes
    FIELD_EMPTY, INVALID_DATA, INVALID_FORMAT,
}
